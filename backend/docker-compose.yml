services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cabruca
    ports:
      - "5432:5432"
    volumes:
      - cabruca_postgres_data:/var/lib/postgresql/data

volumes:
  cabruca_postgres_data:
