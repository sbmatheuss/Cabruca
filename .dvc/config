[core]
    remote = storage
    analytics = false
# Bucket criado em sa-east-1 em 2026-08-01, já com public access block e
# criptografia SSE-S3 habilitados — mesma região do bucket de imagens e do
# User Pool do Cognito (ADR 0004, ADR 0007).
['remote "storage"']
    url = s3://cabruca-dvc-dev-klm
    region = sa-east-1
